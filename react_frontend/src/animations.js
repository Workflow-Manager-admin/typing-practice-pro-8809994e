// Placeholder for optional Framer Motion animation configs.
/**
 * PUBLIC_INTERFACE
 * Animate components using Framer Motion if installed.
 * Example: export basic fade or slide configs here for use.
 */

export const exampleFade = {
  initial: { opacity: 0, y: 12 },
  animate: { opacity: 1, y: 0, transition: { duration: 0.25 } },
  exit: { opacity: 0, y: 16, transition: { duration: 0.18 } }
};
